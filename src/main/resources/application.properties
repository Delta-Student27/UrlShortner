spring.application.name=URLshortner

# ===============================
# Database (Railway MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://${DB_HOST:trolley.proxy.rlwy.net}:${DB_PORT:10811}/${DB_NAME:railway}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:TOXUQbbINSUpXRduETFTyvuvfqvAQCDT}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================
# Server
# ===============================
server.port=${PORT:8082}


# ===============================
# Spring Security
# ===============================
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN
